/**
 * The rotating examples in the resting field.
 *
 * This is what replaced the headline the Community card used to carry. One line
 * per job, in mode order, so the only thing on screen at rest still teaches that
 * the bar plans, answers, writes and reads pictures.
 */
export const PLACEHOLDER_EXAMPLES = [
  'Three days in Lisbon, food and jazz',
  'Is Lisbon safe in November?',
  'We just got back from Peru',
  'Or just drop your screenshots',
];

export const PLACEHOLDER_INTERVAL_MS = 2800;

/** The stable accessible name. The rotating text is decorative and aria-hidden. */
export const COMMAND_BAR_LABEL = 'Ask Navia, plan a trip, or drop screenshots';
